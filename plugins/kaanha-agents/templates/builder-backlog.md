# Overnight Builder — backlog

TEMPLATE — copy to the path you set as `backlog` in `fleet.json`. The
overnight-builder squad takes the TOP unchecked item each night, builds it
on a `builder/*` branch, verifies it, and reports. Keep each task scoped to
one night: one page, one component, one script — never "redesign
everything".

Format: `- [ ] <repo-path> — <task, one sentence, with enough context to act cold>`

## Queue

- [ ] <repo> — <your first scoped task; delete this example line>

## Done

(builder moves completed items here with the date and branch name)
